@model System.Collections.Generic.List<SkeletorDAL.Model.HorseModel>

@{
    ViewBag.Title = "title";
}

<h4></h4>
<div id="horses">
    @foreach (var horse in Model)
    {
        <h1>@Html.ActionLink(@horse.Name, "Index", "HorseProfile", new {id = @horse.ID}, null)</h1>
     
    }
</div>
